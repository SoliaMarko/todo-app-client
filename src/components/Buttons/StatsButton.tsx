import {Button} from '@mui/material';

const StatsButton = () => {
  return (
    <Button sx={{color: '#4cceac'}} variant="contained">
      Add Task
    </Button>
  );
};

export default StatsButton;
